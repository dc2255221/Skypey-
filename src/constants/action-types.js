// File will be responsible for keeping the action type constants
// ** Useful so that you can just import the string from the declared constant. Hence, you need to just declare the constants in one place but can use them in as many places as possible. 

export const SET_ACTIVE_USER_ID = "SET_ACTIVE_USER_ID";

export const SET_TYPING_VALUE = "SET_TYPING_VALUE";

export const SEND_MESSAGE = "SEND_MESSAGE";